{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>

    <div class="row">
        <div class="col">
            {{ form_row(form) }}
        </div>
    </div>
    <button  type="submit">Envoyer</button>
    {{ form_end(form) }}3
{% endblock %}

{% block title %}

{% endblock %}

