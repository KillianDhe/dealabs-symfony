{% extends 'dealForm.html.twig' %}

{% block body %}
  <div class="form-container-preview">
    {{ form_start(form) }}
    <div class="my-custom-class-for-errors">
      {{ form_errors(form) }}
    </div>

    <div class="row">
      <div class="col">
        {{ form_row(form) }}
      </div>
    </div>
    <div class="flex-center form-item__submit">
      <a href="/" class="btn btn-secondary">Cancel</a>
      <button type="submit" class="btn btn-primary">Créer</button>
    </div>
    {{ form_end(form) }}
    <div class="form-container-preview-item">
      <div class="form-container-preview-item__title">
        Preview
      </div>
      {% include('entity/deal/deal_preview.html.twig') %}
    </div>
  </div>
{% endblock %}

{% block title %}

{% endblock %}

